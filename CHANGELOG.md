# Changelog

## Version 1.4.2 - 2026-02-21

### 🔧 การแก้ไข

#### 1. แก้ไขปัญหา Model Cache Directory
- แก้ไขให้ models ถูกดาวน์โหลดไปที่ `$INSTDIR\surya_models` แทนที่จะไปที่ user home directory
- เพิ่มการตั้งค่า environment variables (`HF_HOME`, `TRANSFORMERS_CACHE`) ในตัว installer
- แก้ไข `main.js` ให้ส่ง environment variables ไปยัง Surya server
- แก้ไขให้ Surya server ใช้ model cache directory ที่ถูกต้อง

#### 2. ปรับปรุง NSIS Installer
- เพิ่มการสร้างโฟลเดอร์ `surya_models` อัตโนมัติ
- ตั้งค่า environment variables ก่อนดาวน์โหลด models
- แสดงข้อความบอกตำแหน่งที่ models ถูกบันทึก
- ปิดการแสดง symlink warning บน Windows

#### 3. ปรับปรุง Surya Server
- กรอง FutureWarning messages ออกจาก error logs
- แสดงเฉพาะ error ที่สำคัญ

### 📝 การอัปเดตเอกสาร

- อัปเดต CHANGELOG.md พร้อมคำแนะนำการ rebuild

---

## Version 1.4.1 - 2026-02-21

### 🔧 การปรับปรุง

#### 1. บังคับใช้ GPU เมื่อตรวจพบ
- เมื่อตรวจพบ GPU (CUDA หรือ MPS) จะบังคับใช้ GPU เท่านั้น
- ไม่ fallback เป็น CPU เมื่อมี GPU
- แสดงข้อความยืนยันว่า GPU จะถูกใช้สำหรับทุก operations
- ใช้ float16 บน CUDA GPU เพื่อความเร็วสูงสุด

#### 2. เพิ่มบล็อกแสดง Device แยก
- เพิ่ม section "อุปกรณ์ประมวลผล" ใน sidebar
- แสดงไอคอนและชื่อ device ที่ใช้งาน
- สีเขียวสำหรับ GPU, สีน้ำเงินสำหรับ CPU
- แสดงเฉพาะเมื่อเลือก Surya OCR

### 📝 การอัปเดตเอกสาร

- อัปเดต INSTALL_CUDA.md เพื่อเน้นว่า GPU จะถูกใช้อัตโนมัติ
- เพิ่มคำเตือนว่าเมื่อมี GPU จะไม่ fallback เป็น CPU

---

## Version 1.4.0 - 2026-02-21

### ✨ คุณสมบัติใหม่

#### 1. GPU Acceleration (เร่งความเร็วด้วย GPU)
- ตรวจสอบและใช้ GPU อัตโนมัติถ้ามี
- รองรับ NVIDIA GPU (CUDA)
- รองรับ Apple Silicon (MPS)
- เร็วขึ้น 5-10 เท่าเมื่อใช้ GPU
- แสดงข้อมูล GPU ที่ใช้งานใน console
- ใช้ half precision (float16) บน GPU เพื่อความเร็ว

#### 2. แสดง Device Info ในสถานะ
- แสดงว่ากำลังใช้ CPU หรือ GPU
- แสดงไอคอน: 🚀 GPU (CUDA), ⚡ GPU (Apple Silicon), 💻 CPU
- แสดงระหว่างเลือก Surya OCR
- แสดงระหว่างประมวลผล OCR

### 🔧 การปรับปรุง

- เพิ่มการตรวจสอบ device และ dtype
- แสดงข้อมูล GPU memory
- Fallback เป็น CPU ถ้าไม่มี GPU
- เพิ่มฟังก์ชัน getDeviceDisplayText() สำหรับแสดงข้อมูล device

### 📦 ไฟล์ใหม่

- `INSTALL_CUDA.md` - คู่มือติดตั้ง CUDA สำหรับ NVIDIA GPU

---

## Version 1.3.0 - 2026-02-21

### ✨ คุณสมบัติใหม่

#### 1. Image Lightbox (ขยายรูป)
- คลิกที่รูปภาพเพื่อขยายดูแบบเต็มหน้าจอ
- ปุ่มปิด (×) ที่มุมขวาบน
- กด ESC หรือคลิกพื้นหลังเพื่อปิด
- รองรับทั้งรูปใน preview area และ batch results
- Animation fade in/zoom in ที่นุ่มนวล

### 🔧 การปรับปรุง

- เพิ่ม cursor pointer เมื่อ hover รูปภาพ
- ปิด DevTools ที่เปิดอัตโนมัติ

---

## Version 1.2.1 - 2026-02-21

### ✨ คุณสมบัติใหม่

#### 1. Loading Indicator
- เพิ่ม loading animation เมื่อกดปุ่ม "ดึงข้อความจากรูป"
- แสดงไอคอน ⏳ และข้อความ "กำลังดึงข้อความ..."
- ปุ่มจะ disabled ระหว่างประมวลผล

### 🔧 การปรับปรุง

#### 1. แถบเลื่อน (Scrollbar)
- ลดขนาดแถบเลื่อนให้เล็กลง (8px)
- ใช้สีตามธีมที่เลือก
- รองรับทั้ง Chrome และ Firefox

#### 2. ธีมเริ่มต้น
- ธีมเริ่มต้นจะตามการตั้งค่าระบบ (System Preference)
- ถ้าระบบเป็นโหมดมืด → แอปจะเป็นโหมดมืด
- ถ้าระบบเป็นโหมดสว่าง → แอปจะเป็นโหมดสว่าง
- อัปเดตอัตโนมัติเมื่อเปลี่ยนธีมระบบ (ถ้ายังไม่เคยเลือกธีมเอง)

---

## Version 1.2.0 - 2026-02-21

### ✨ คุณสมบัติใหม่

#### 1. โหมดมืด/สว่าง
- เพิ่มปุ่มสลับโหมดมืด/สว่าง
- จำโหมดที่เลือกไว้ใน localStorage
- โหมดมืด: สีเข้ม สบายตา (ค่าเริ่มต้น)
- โหมดสว่าง: สีสว่าง ชัดเจน

#### 2. ปรับปรุง UI ของ Sidebar
- เพิ่มความกว้าง sidebar เป็น 300px
- ใช้ card design สำหรับแต่ละ section
- ปรับ spacing และ padding ให้สวยงามขึ้น
- ปรับ button styles ให้ทันสมัย
- เพิ่ม hover effects และ transitions
- ปรับ input/select styles ให้ดูดีขึ้น
- ปรับสี extract button เป็นสีเขียว

### 🔧 การปรับปรุง

- ปรับ font sizes ให้อ่านง่ายขึ้น
- เพิ่ม box shadows สำหรับ buttons
- ปรับ border radius ให้นุ่มนวล
- เพิ่ม focus states สำหรับ accessibility
- ปรับ checkbox และ label styles

---

## Version 1.1.0 - 2026-02-21

### ✨ คุณสมบัติใหม่

#### 1. จำขนาดและตำแหน่งหน้าต่าง
- แอปจะจำขนาดและตำแหน่งหน้าต่างที่คุณปรับไว้
- เปิดแอปครั้งต่อไปจะกลับมาที่ขนาดและตำแหน่งเดิม
- รองรับการ maximize/restore
- ขนาดเริ่มต้น: 80% ของหน้าจอ (ไม่เกินหน้าจอ)

#### 2. บันทึกประวัติถาวร
- ประวัติการทำ OCR จะถูกบันทึกใน localStorage
- ไม่หายเมื่อปิดแอป
- เปิดแอปใหม่จะเห็นประวัติเดิม
- รองรับสูงสุด 50 รายการ

### 🔧 การปรับปรุง

- ปรับขนาดหน้าต่างเริ่มต้นให้เหมาะสมกับหน้าจอ
- ป้องกันหน้าต่างออกนอกหน้าจอ
- เพิ่ม electron-store สำหรับจัดการ settings

### 📦 Dependencies ใหม่

- electron-store@^8.1.0 - สำหรับจัดการ window state

---

## Version 1.0.0 - 2026-02-20

### คุณสมบัติหลัก

- ดึงข้อความจากรูปภาพ (JPG, PNG)
- ดึงข้อความจาก PDF
- รองรับ OCR 2 แบบ: Tesseract และ Surya
- รองรับภาษาไทยและอังกฤษ
- โหมดโค้ดสำหรับอ่านโค้ดให้แม่นยำ
- วางรูปหลายรูปพร้อมกัน
- ประวัติการทำ OCR
- ส่งออกเป็นไฟล์ .txt
